import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { Toaster as SonnerToaster } from "@/components/ui/sonner";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Koottam Cart — AI Community Commerce Platform",
  description:
    "AI-native community commerce and agri-supply-chain platform. Weather → Demand → Farming → Supply → Pricing → Logistics → Customers → Payments → Intelligence, coordinated by AI.",
  keywords: [
    "Koottam Cart",
    "AI agriculture",
    "community commerce",
    "supply chain",
    "Tamil Nadu",
    "SHG",
    "farmer",
    "demand forecast",
  ],
  authors: [{ name: "Koottam Cart" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
          <SonnerToaster position="top-right" richColors />
        </ThemeProvider>
      </body>
    </html>
  );
}
